diff a/Camera/CameraMove.cs b/Camera/CameraMove.cs	(rejected hunks)
@@ -77,7 +77,8 @@
         if (Input.GetKeyDown(KeyCode.Tab) && uitype == UItype.UI)
         {
             uitype =UItype.Play;
-            Tabfalse();
+            menu.SetActive(false);
+            isTabactive = false;
         }
         else if (Input.GetKeyDown(KeyCode.Tab) && uitype == UItype.Play)
         {
